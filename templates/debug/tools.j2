{% extends "base.j2" %}

{% block content %}
<div class="px-5 flex flex-col mt-2 flex-start gap-2">
    <div class="p-6 self-center text-center border-red-500/40 rounded-lg border bg-red-500/10">
        <h1 class="text-xl font-bold mb-2">POZOR</h1>
        <p>Zajímavá tlačítka na této stránce jsou určena pro vývojáře (tj. Čýmse).
        Pokud na něco kliknete když nemáte, je možné že něco rozbijete.
        Pokud kliknete na tlačítko se symbolem <i class="bi bi-exclamation-triangle text-lg mr-2"></i>tak <b>definitivně</b> něco rozbijete (taky se to zapisuje do logů, takže budu vědět, žes to byl ty).
        Prosím, nepřidělávejte mi práci :agrlove:</p>
    </div>
    <div class="flex-row flex gap-4 flex-wrap">
        <div class="button-group">
            <h1 class="text-lg font-bold">Metadata</h1>
            <a class="glass-btn w-fit" href="{{url_for('DebugTools.updaterss')}}"><i class="bi bi-rss text-lg mr-2"></i><p class="select-none">Vynutit aktualizaci RSS</p></a>
            <a class="glass-btn w-fit" href="{{url_for('DebugTools.avdownload')}}"><i class="bi bi-image text-lg mr-2"></i><p class="select-none">Vynutit aktualizaci avatarů</p></a>
            <a class="glass-btn w-fit" href="{{url_for('DebugTools.nickupdate')}}"><i class="bi bi-person text-lg mr-2"></i><p class="select-none">Vynutit aktualizaci uživatelů</p></a>
        </div>
        <div class="button-group">
            <h1 class="text-lg font-bold">Databáze</h1>
            <a class="glass-btn w-fit" href="{{url_for('DebugTools.export_database')}}"><i class="bi bi-database text-lg mr-2"></i><p class="select-none">Exportovat databázi</p></a>
        </div>
        <div class="button-group">
            <h1 class="text-lg font-bold">Zálohy</h1>
            <div class="glass-btn w-fit"><i class="bi bi-key text-lg mr-2"></i><p class="select-none">Otestovat přihlášení k Portaineru</p></div>
            <div class="btn-danger w-fit"><i class="bi bi-exclamation-triangle text-lg mr-2"></i><p class="select-none">Vynutit start WikiComma kontejneru</p></div>
            <div class="btn-danger w-fit"><i class="bi bi-exclamation-triangle text-lg mr-2"></i><p class="select-none">Ukončit WikiComma kontejner</p></div>
        </div>
        <div class="button-group">
            <h1 class="text-lg font-bold">Discord</h1>
            <a class="glass-btn w-fit" href="{{url_for('DebugTools.webhook_testing')}}"><i class="bi bi-discord text-lg mr-2"></i><p class="select-none">Otestovat webhook</p></a>
        </div>
    </div>
</div>
{% endblock content %}